spring.application.name=due-diligence-agent

# ==========================
# Database Configuration
# ==========================
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/real_estate_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:knmdp}

# ==========================
# JPA Configuration
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================
# Server Configuration
# ==========================
server.port=8080

# ==========================
# JWT Configuration
# ==========================
jwt.secret=${JWT_SECRET:dsF+8c9sEWPInYLhAIbrNidHtVoB0g5cNx6PGEwTFd4Iolg9qrz7oIUwiBM44yUQC1DnXa54XRK05srlvr/FyA==}
jwt.expiration=${JWT_EXPIRATION:3600000}

# ==========================
# Logging
# ==========================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=TRACE

# ==========================
# Gmail SMTP Configuration
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.username=${MAIL_USERNAME:astitvarajfbg@gmail.com}
spring.mail.password=${MAIL_PASSWORD:luen xlll lifx wmlb}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

google.client.id=520450322801-uksrpeu3m2hgkc9pqp1lhj64fo9dioro.apps.googleusercontent.com

# ==========================
# Address Validation (real geocoding lookup via OpenStreetMap Nominatim,
# free / no API key). Override nominatim-url to point at a self-hosted
# instance or a paid provider if you outgrow Nominatim's 1 req/sec policy.
# ==========================
address.validation.nominatim-url=${ADDRESS_VALIDATION_URL:https://nominatim.openstreetmap.org/search}
address.validation.user-agent=${ADDRESS_VALIDATION_USER_AGENT:DueDiligenceAgent/1.0}

# ==========================
# Profile Photo Upload (Requested Change 4)
# ==========================
# Local disk folder (relative to the app's working directory) that uploaded
# avatars are written to and served from -- see WebConfig / UserService.
app.upload.profile-images-dir=uploads/profile-images
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB